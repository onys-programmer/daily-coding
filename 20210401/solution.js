function solution(new_id) {
  // 1. 3 <= id <= 15
  // 2. 'a', '1', '-', '_', '.', 
  // 3. '.' != first && '.' != end 
  // 4. if(index(n)='.') {
  //  index(n-1) != ',' && index(n+1) != ','
  // }
  const lowerId = new_id.toLowerCase();
  
  

}

test('solution', () => {

});