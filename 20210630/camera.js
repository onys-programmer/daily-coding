function camera(routes) {
  // 차별 이동거리 짧은 순으로 sort된 배열 반환
  const sortByDistance = (arr) => {
    return sorted;
  };
  // sort된 배열 저장
  let sortedByDistance = sortByDistance(routes);

  // sort된 배열 돌면서 camera 설치해. 

  // 왼쪽 오른쪽 중 어느 쪽이 더 많이 만나는지 판단하는 함수
  const checkLorR = () => {

  }

  // 각 자동차가 카메라를 만났는지 확인하는 행동

  // 카메라 다 만났으면 카메라 개수 리턴

  

  let minCamera;
  return minCamera;
}

// 모든 차량이 최소 한 번
// routes 매개변수. 최소 몇대
// [[-20,15],[-14,-5],[-18,-13],[-5,-3]]
// 